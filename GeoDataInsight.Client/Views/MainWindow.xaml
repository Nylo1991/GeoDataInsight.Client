<Window x:Class="GeoDataInsight.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        Title="GeoDataInsight - Inteligência Geográfica" Height="768" Width="1366"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" Background="#E5E7EB">

    <Grid>
        <mapsui:MapControl x:Name="mapControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Panel.ZIndex="0"/>

        <Border HorizontalAlignment="Left" VerticalAlignment="Top" Width="420" MaxHeight="680" 
                Margin="25" Background="#FAFFFFFF" CornerRadius="16" Panel.ZIndex="1"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.2" Color="#000000"/>
            </Border.Effect>

            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,25">
                    <TextBlock Text="GeoDataInsight" FontSize="26" FontWeight="Black" Foreground="#111827"/>
                    <TextBlock Text="Encontre localizações precisas no mapa" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="10" Height="55" 
                        BorderBrush="#D1D5DB" BorderThickness="1" Padding="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="📍" VerticalAlignment="Center" Margin="15,0,10,0" FontSize="18"/>

                        <TextBox Grid.Column="1" Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}" 
                                 BorderThickness="0" Background="Transparent" FontSize="16" 
                                 VerticalContentAlignment="Center" Foreground="#1F2937"/>

                        <Button Grid.Column="2" Command="{Binding BuscarCommand}" 
                                Background="#2563EB" Foreground="White" BorderThickness="0" Width="45" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                            <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                        </Button>
                    </Grid>
                </Border>

                <ListBox Grid.Row="2" ItemsSource="{Binding Resultados}" SelectedItem="{Binding Selecionado}" 
                         BorderThickness="0" Background="Transparent" Margin="0,20,0,0" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Cursor" Value="Hand"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFFFF" CornerRadius="10" Padding="15" 
                                    BorderBrush="#E5E7EB" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Logradouro}" FontWeight="Bold" FontSize="16" Foreground="#111827" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Bairro}" FontSize="14" Foreground="#4B5563" Margin="0,5,0,0" TextWrapping="Wrap"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Border Background="#DBEAFE" CornerRadius="5" Padding="6,3">
                                            <TextBlock Text="{Binding Cep}" FontSize="12" FontWeight="SemiBold" Foreground="#1D4ED8"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>